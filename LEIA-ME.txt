PROJETO DOCENTES UFERSA — EDIÇÃO LOCAL

1. Extraia o ZIP e abra a pasta docentes-ufersa no VS Code.
2. Com Python 3 instalado, abra o terminal do VS Code nessa pasta.
3. Execute no Windows:
   py -m http.server 8000 --directory dist
4. Abra http://localhost:8000 no navegador.
   Encerre com Ctrl+C no terminal.

ARQUIVOS
- dist/index.html: página inicial.
- dist/denge/index.html: docentes do DENGE.
- dist/dceti/index.html: docentes do DCETI.
- dist/dch/index.html: docentes do DCH.
- dist/style.css: cores, tipografia e layout.
- dist/photos/: fotos usadas nos cartões.
- data.tsv: dados dos docentes, separados por tabulações.
- build.py: gera as quatro páginas a partir dos dados e do modelo.
- photos.json: associação de cada docente à sua foto.
- status.json: campos com limitações na consulta original.
- photo-sources.json: URLs de origem das fotos.

EDIÇÃO
Para mudar as cores e a aparência, edite dist/style.css.
Para mudar dados dos docentes, edite data.tsv e execute:
   py build.py
Para mudar a estrutura dos cartões ou da página inicial, edite build.py
e execute o mesmo comando.
Também é possível editar os HTML diretamente, mas essas alterações
serão substituídas caso você execute build.py novamente.

Os caminhos de navegação, estilos e fotos são relativos, compatíveis com
o servidor local e com a publicação em subpastas, como no GitHub Pages.
No Pages, publique o conteúdo de dist como raiz do site.

PUBLICAÇÃO AUTOMÁTICA NO GITHUB PAGES
1. No repositório do GitHub, abra Settings > Pages.
2. Em Build and deployment > Source, selecione GitHub Actions.
3. Envie .github/workflows/pages.yml e as alterações do projeto para main.
4. Na aba Actions, acompanhe "Publicar site no Pages" até concluir.
5. O endereço publicado aparece em Settings > Pages.

A cada push na main, o GitHub executa build.py e publica o conteúdo de dist.
Para publicar manualmente, abra Actions > Publicar site no Pages > Run workflow.
Altere os dados em data.tsv e a estrutura das páginas em build.py.
Alterações feitas diretamente nos HTML são substituídas na publicação.
Mantenha dist/style.css e dist/photos no repositório: o gerador usa esses arquivos.
Editar localmente só altera o site publicado após enviar as mudanças ao GitHub.

Esta cópia exportada não contém credenciais nem o histórico Git.
A versão publicada permanece inalterada.


CHECAGEM DI?RIA DOS DEPARTAMENTOS
O workflow consulta as tr?s listas p?blicas do SIGAA diariamente ?s 00:00
em Fortaleza (03:00 UTC). O GitHub pode atrasar o in?cio da execu??o.
Tamb?m ? poss?vel consultar agora em Actions > Publicar site no Pages > Run workflow.
Envios comuns para main publicam o projeto sem consultar novamente o SIGAA.

A automa??o compara docentes pelo SIAPE e departamento. Novas entradas aparecem
na pr?xima publica??o bem-sucedida. Uma aus?ncia s? remove um cart?o ap?s duas
checagens completas, separadas por pelo menos seis horas (normalmente dois dias).
Se o docente reaparecer, a aus?ncia pendente ? cancelada. Durante a confirma??o
uma transfer?ncia pode aparecer temporariamente nos dois departamentos.
Uma troca simult?nea ? registrada como entrada e sa?da, sem presumir que uma
pessoa foi contratada para substituir outra.

Se uma lista falhar, estiver vazia, tiver identificadores repetidos, total
inconsistente ou perder muitos docentes de uma vez, o workflow falha e mant?m
a publica??o anterior. Veja o erro na aba Actions. Quedas maiores que 20% do
quadro (com toler?ncia m?nima de tr?s aus?ncias) exigem revis?o do coletor/dados.

O rodap? informa a ?ltima checagem das listas. A data dos perfis e contatos ?
separada, pois a rotina n?o atualiza os perfis individuais. Biografias, fotos e
contatos j? cadastrados s?o preservados. Para novos docentes, s?o importados
nome, titula??o, forma??o, v?nculo e Lattes quando dispon?veis na lista p?blica.
Contatos e fotos n?o s?o coletados automaticamente: os cart?es mostram os campos
n?o informados e as iniciais at? que esses dados sejam complementados.

Com a p?gina aberta, o navegador consulta version.json a cada cinco minutos e
ao retornar ? aba. Se houver outra vers?o, aparece o bot?o "Atualizar p?gina".
A checagem di?ria sem mudan?a de conte?do n?o produz esse aviso. O rodap? tamb?m
indica quando a ?ltima consulta bem-sucedida tem mais de tr?s dias.

O workflow grava data.tsv e sync-state.json na main usando o GITHUB_TOKEN padr?o;
n?o ? necess?rio cadastrar senha ou token pessoal. A publica??o ocorre no mesmo
workflow. Regras de prote??o da branch que pro?bam esse commit precisam ser
adaptadas para permitir a automa??o; uma rejei??o interrompe a publica??o.
Antes de editar localmente ap?s uma atualiza??o autom?tica, sincronize a main
com git pull --ff-only. Os HTML publicados s?o gerados no Actions, e podem ser
mais recentes que os HTML versionados em dist.

Para ativar as novidades, envie todos os arquivos alterados/criados ao GitHub e
execute o workflow manualmente uma vez. Mantenha Settings > Pages > Source como
GitHub Actions. O agendamento exige que o workflow esteja na branch padr?o main.
O GitHub pode desativar agendamentos em reposit?rios p?blicos ap?s 60 dias sem
atividade; confira Actions se a data de checagem do site parar de avan?ar.

Valida??o local:
  python -X utf8 -m unittest discover -s tests -v
  python -X utf8 sync_sigaa.py --dry-run
  python -X utf8 build.py

Refer?ncia do agendamento:
https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows#schedule
